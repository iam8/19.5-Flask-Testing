{# Template for homepage of Boggle app. #}

{% extends "base.jinja2" %}

{% block content %}

    {% for row in board %}
        <div>{{ row }}</div>
    {% endfor %}

    <p id="num-games">Total games played: {{ session.get("num_games", "N/A") }}</p>
    <p id="highest-score">Highest score: {{ session.get("max", "N/A") }}</p>
    <p id="score-total">Current score total: 0</p>

    <h3>Guess a word!</h3>
    <p id="time-remaining"></p>

    {# Form for submitting a word guess #}
    <form id="guess-form" method="POST">
        <label for="word-guess">Guess:</label>
        <input id="word-input" type="text" name="word-guess" placeholder="Enter a word">

        <button id="submit-button">Submit</button>
    </form>

    <div id="guess-display"></div>
    <div id="validity-msg"></div>

{% endblock content %}
